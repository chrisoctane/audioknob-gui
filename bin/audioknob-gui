#!/usr/bin/env bash
set -euo pipefail

# For development, set AUDIOKNOB_DEV_REPO to the repo path
if [ -n "${AUDIOKNOB_DEV_REPO:-}" ]; then
    export PYTHONPATH="${AUDIOKNOB_DEV_REPO}:${PYTHONPATH:-}"
fi

exec python3 -m audioknob_gui.gui.app "$@"
